services:
  - type: web
    name: nocturne
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python venice_server.py
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
    healthCheckPath: /health
